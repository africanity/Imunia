import 'package:flutter/material.dart';
import '../../core/theme/app_colors.dart';

class ChildrenSelectorScreen extends StatelessWidget {
  const ChildrenSelectorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Changer d\'enfant'),
      ),
      body: const Center(
        child: Text('À implémenter'),
      ),
    );
  }
}

